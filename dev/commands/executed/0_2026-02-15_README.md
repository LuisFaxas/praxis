# Executed Commands

Log of commands run during work sessions. Each entry records what was executed, when, and why.

## Naming Convention

`{number}_{YYYY-MM-DD}_{DESCRIPTION}.md`

- Number is sequential and chronological (0, 1, 2, ...)
- Date is the execution date in ISO format
- Description is uppercase, underscore-separated
